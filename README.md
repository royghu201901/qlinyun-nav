# qlinyun-nav

## Build Setup

```bash
# install dependencies
$ pnpm install

# serve with hot reload at localhost:8000
$ pnpm dev

# build for production and launch server
$ pnpm build
$ pnpm start

```
